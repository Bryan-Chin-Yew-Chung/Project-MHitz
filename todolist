Keep form data after signup failure

Check password also when admin login // because hashed fk it up

Add more options to usernames now currently a-z 0-9

change all PHP echo into normal html (big revelation)
